#include <stdio.h>
#include <math.h>

/*Escreva um programa que leia um número inteiro do teclado e diga se o número lido
é par ou ímpar.*/

void main()
{
	int x;
	printf("Digite um numero inteiro para saber se ele eh par ou impar:");
	scanf("%f", &x);
	
	x = x%2;
	
	if (x = 0)
		printf("O numero inserido eh par");
	
	else (x = 1);
		printf("O numero inserido eh impar");
}
